# AI 検討メモ (plan_by_agent)

このファイルはAIがアイデアや実装計画、既存コードの分析結果を書き溜めるためのメモ帳です。

---

## 現在の状態（2026-03）

Phase 1 完了。基盤システム・キャラクター描画・ステージ・UI・ゲームフローすべて実装済み。

---

## 次のステップ

### A. キャラクター見た目の微調整（優先：高）

1. **屈み+正面向きの表示バグ修正**
   - 屈んだ状態で正面を向くと描画が崩れる問題
   - `CharacterDrawer.gd` の `_draw_front_back()` と crouch 状態の組み合わせを調査・修正

2. **手の形状を楕円→長方形に変更**
   - `CharacterDrawUtils.gd` の `draw_hand()` を矩形ベースに変更

3. **頭を真円に変更**
   - `CharacterDrawUtils.gd` の `draw_ellipse()` のアスペクト比を 1:1 に
   - ユーザより：ただし、内部で計算に使う時の頭の横幅は、既存と同じものを使うこと

4. **腕が短い問題の調査・修正**
   - `armLength = head * 2.7` の係数を見直し

5. **側面図の腕の位置調整**
   - `CharacterDrawer.gd` の `_draw_side()` 内の腕描画座標を調整

5. **絵文字の文字化け修正**
   - ゲーム内で絵文字が正しく表示されない問題

6. **ステージの「目標高さ」表示を削除**
   - 不要な表示を削除し、改行で画面が動く不快な挙動を解消

### B. 家ステージの充実（優先：中）

仕様書 `specs/stage_room.md` を先に作ってから実装する方針。

1. レンジフードの描画見直し（`StageBuilder.gd`）
2. キッチン（カウンター・コンロ・シンク）の障害物配置
3. 机の配置
4. 背景の充実（壁・床・窓・外の景色）

### C. 将来の展望（低優先）

- ゲーム性の確立（ライフイベント: 保育園 → 小学校 → 中学校）
- ステージ追加（電車・屋外・学校）
- アニメーション拡張（AnimationPlayer / Skeleton2D 導入検討）

---

## 新規機能のアイデアと実装見積もり（2026-03）

ご主人様（ユーザ）のメモに基づき、これからの大きな方向性の実装規模・必要なタスクを見積もりました。
全体的に大掛かりになる見込みですので、どこから着手するか、あるいは一部だけ（「セーブ機能の基礎だけ先にする」など）切り出すか、添削とご検討をお願いいたします。

### 提案1：シームレスな「導入」フローと「お出かけ」
- **概要**: タイトル画面 → キャラ作成 → 自室（ホーム）を暗転なしで繋ぎ、ステージ選択はゲーム内のスマホや玄関ドアから行う。
- **実装規模**: **大**（シーン管理の変更と、UIの根本的な改修が必要）
- **必要な実装タスク**:
  1. `TitleScene` への「自室背景」と「ベッドで寝ているアバター」の描画組み込み。
  2. タイトル → キャラ作成へのUIのシームレスな切り替え（カメラ移動、アバターの立ち上がりアニメーションやポーズ演算など）。
  3. モード切替ではなく、常にゲーム空間を保つためのシーンマネジメント改修。
  4. ホームステージに「玄関ドア」を配置し、インタラクション（触ると外へ行く）を追加。
  5. 既存の `StageSelectScene` を「スマホ画面」として作り直し、ゲームプレイ中にいつでも呼び出せるように変更。

### 提案2：設定拡張と「セーブ機能」
- **概要**: 学年による身長プリセット・服装変更の実装と、キャラクターデータのローカル保存・ロード機能。
- **実装規模**: **中〜大**（データ管理基盤の作成と、描画パターンの追加）
- **必要な実装タスク**:
  1. セーブ・ロード処理機能（`FileAccess`を用いたJSON保存など）の新規実装。
  2. タイトル画面への「続きから」ボタン追加と、起動時の状態復元プロセスの構築。
  3. `CharacterCreatorScene` への「学年」などの選択UIの追加（学年に応じたプリセットの用意）。
  4. `CharacterDrawer.gd` に、「服装（制服・ジャージ・私服など）」に応じた描画ロジックの追加（※部位ごとに服の色や形を描き分ける必要があり、描画コードの大幅な加筆が予想されます）。

### 提案3：新アクション「体育座り」とホームステージ拡張
- **概要**: ホームステージを充実させ、体育座りなどの新ポーズや、家具との触れ合いを追加する。
- **実装規模**: **中**（既存のシステムを拡張していく形のため、段階的に可能です）
- **必要な実装タスク**:
  1. `CharacterPoseCalculator` および `CharacterDrawer.gd` に、新ポーズ（体育座り、寝る等）の関節計算・描画ロジックを追加。
  2. `StageBuilder.gd` で自室ステージの拡張（ベッド、机、椅子などのオブジェクト構築）。
  3. 家具とのインタラクション処理の追加（例：ベッドを調べると「寝る」ポーズに移行するロジック）。

---

## 開発メモ

### 身体パラメータの計算式

`Global.gd` で計算している各部位の比率：
- `head = height / ratio`
- `leg = height * legRatio / 100`
- `arm = height - leg - head - (neck * 2)`
- `armLength = head * 2.7`

### 屈みの二分探索

`CharacterPoseCalculator.gd` 内で、目標の高さ（px）を満たす最小の `t` (ポーズパラメータ) を 15 回の反復で探索。
- `t` (0~1): 直立からかがみ（背中を丸める）
- `t` (1~2): 更に深く屈む（膝を曲げる）
