# AI 検討メモ (plan_by_agent)

このファイルはAIがアイデアや実装計画、既存コードの分析結果を書き溜めるためのメモ帳です。

---

## 現在のステータス（2026-03-08 更新）

### 完了済み ✅
- 基盤システム、体の基本描画（横・正面・顔の向き対応）
- 基本的なUI構築、ゲームフロー（タイトル→キャラクリ→メイン）
- ステージ間の双方向な移動アクション（シームレスな遷移基盤）
- ESCメニューZ-orderバグ修正、ステータスオーバーレイ化、IntroScene導入
- 比較対象となるNPCの実装・配置 (SkeletalNPC.tscn)
- 学校廊下ステージ（school_hallway）追加
- 保健室ステージ（infirmary）追加・身長計・体重計の描画
- **ステージ大拡充（2026-03-08）**:
  - `outdoor`：郵便ポスト・信号機・軽自動車・バス停を追加
  - `door_to_train` → `door_to_station` に変更し駅フローを整理
  - 新ステージ `schoolyard`（校庭）：鉄棒・ジャングルジム・バスケゴール・サッカーゴール
  - 新ステージ `station`（駅）：自動改札・ベンチ・時刻表・自販機

### 現在の遷移フロー
```
家(room) ⇔ 自室(myroom)
  ↕
屋外(outdoor) ⇔ 駅(station) ⇔ 電車(train)
  ↕
学校廊下(school_hallway) ⇔ 教室(school)
  ↓          ↓
 校庭      保健室
(schoolyard) (infirmary)
```

---

## 次のアクション候補（Next Actions）

### 優先度 高 ⭐⭐⭐

#### A. 教室の年齢パラメータ切り替えシステム
**背景**: 現在 `school` ステージは固定レイアウトのみ。設計書では保育園〜高校で机サイズ・天井高が変わる想定。
**実装方針**:
- `StageBuilder.build_stage(stage_id, parent, cm_to_px)` に `age: int` 引数を追加
- 年齢に応じて obstacles リストを動的に差し替える
- 対象ステージ: `school`（`schoolyard` も将来的に）

| 年齢 | ステージ相当 | 机高さ | 天井高 |
|------|------------|--------|--------|
| 3-5  | 保育園      | 45cm  | 260cm  |
| 6-12 | 小学校      | 60cm  | 300cm  |
| 13-15| 中学校      | 72cm  | 300cm  |
| 16-18| 高校        | 76cm  | 300cm  |

#### B. ゲームフロー：学年・年齢システムの構築
**背景**: キャラクリで学年を選べるようにすると、ステージの机サイズ等が連動する。
**実装方針**:
- `Global.gd` に `age: int` / `school_grade: String` を追加
- キャラクリ画面に学年選択UIを追加
- `MainScene` でステージ遷移時に年齢パラメータを渡す

---

### 優先度 中 ⭐⭐

#### C. キャラクター描画の洗練
- 関節部の隙間をなくす（Line2D化 or 半円補間）
- 足の形状改善（横から見た楔形）

#### D. 屋外オブジェクトの追加
- 自転車（バイク）・電柱・カーブミラー（230cm）
- 現状 width=1200cm で余裕がないため、width 拡張も検討

---

### 優先度 低 ⭐

#### E. 両親の身長設定と遺伝システム
- 父母身長 → 主人公の成長曲線への影響

#### F. ローカルセーブ機能の拡充
- 現在はスロットセーブのみ。成長グラフの保存・表示

---

## 開発メモ

### 身体パラメータの計算式 (`Global.gd`)
- `head = height / ratio`
- `leg = height * legRatio / 100`
- `arm = height - leg - head - (neck * 2)`
- `armLength = head * 2.7`

### 屈みの二分探索 (`CharacterPoseCalculator.gd`)
目標の高さ（px）を満たす最小の `t` (ポーズパラメータ) を反復探索。
- `t` (0~1): 直立からかがみ（背中を丸める）
- `t` (1~2): 更に深く屈む（膝を曲げる）

### ステージ遷移の仕組み (`MainScene.gd`)
- `door_to_XXX` → `"XXX"` を自動解決して遷移
- 戻り口ドア `door_to_<from_stage>` の近くにスポーン
- 追加ステージは STAGES 辞書に定義するだけで自動対応
