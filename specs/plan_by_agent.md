# AI 検討メモ (plan_by_agent)

このファイルは、分析ドキュメント（`misc/` 以下のリサーチ結果）に基づき、本作を「高身長シミュレーター」として面白くするための具体的な戦略と実装計画を書き溜める場所ですわ。

---

## 💎 コア戦略：高身長体験の「フェティシズム」と「リアリティ」
分析の結果、本作が優先すべき「面白さ（快感・フック）」は以下の4点に集約されますわ。

1. **数値の執着**: cm単位の変化、平均との比較、成長ログ。
2. **日常の摩擦**: ドアをくぐる、低い洗面台を使う、頭上の障害物を避ける不便さ。
3. **視線の格差**: 普通の人々（NPC）を「見下ろす」視線の快感と、相手からの「驚き」の視線。
4. **ライフステージの変遷**: 成長に伴って、同じ世界（家・学校）が「小さくなっていく」体験。

---

## 🚀 次のアクションプラン (Next Actions)

### 🔥 フェーズ3：身体的変化の深化と日常の「摩擦」

#### 3-B. コアNPC「はるか」とのコミュニケーション深化 ⭐⭐
- **現状**: 会話はできるが、まだ単発。
- **目標**:
    - 身長差（10cm差、30cm差、50cm差など）に応じたセリフ分岐。
    - 相手を見下ろす、見上げる際の見切れ感や視線の交差（LookAt制御の強化）。
    - 学期進行（成長）に合わせて「はるか」の反応が変化する。

#### 3-D. 環境とのインタラクト強化 ⭐⭐
- **実装案**:
    - 手洗い場、机、椅子の「低さ」に対するインタラクト時の専用メッセージ（「腰が痛い……」「低すぎる……」）。
    - 低いドアをくぐる際の「屈む」動作に付随するスタミナ消費や、精神的疲労（ストレス値）の検討。

#### 3-E. 受動的ストレス・イベントの追加 ⭐⭐
- **集合写真**: 背が高すぎてフレームアウトする、あるいは一番後ろで顔しか映らない悲哀。
- **雨の日の傘**: NPCの傘が自分の顔の高さにあり、避けるのが大変（物理的な邪魔さ）。

---

## 📓 実装メモ（テクニカル）

- **センサー拡張**:
    - つり革（overhead判定）が「顔（目の位置）」の高さにある場合、回避アニメーションを優先。
- **イベント管理**:
    - `Global.pending_events` を活用し、特定の学期や特定の身長達成時にイベントを差し込む。

---

## 📊 実装工数比較（2026-03-09 見積もり）

> 工数定義：**S**=1〜2h / **M**=半日〜1日 / **L**=1〜2日以上

### 前提：コード調査で判明した既存資産

| 項目 | 状態 |
|---|---|
| `honoka`(155cm)・`senior`(168cm)・`mother`(158cm)・`father`(170cm) | **Global.gd に定義済み**（配置・会話のみ未実装） |
| `measure_invite` / `measure_after` ダイアログ | **部分実装済み** |
| `pending_events` キュー | **実装済み**（学期連動トリガーに流用可能） |
| NPC の `look_pitch`（見上げ角度） | **実装済み**（主人公側の見下ろしのみ未実装） |
| `taiiku_suwari` ポーズ | **実装済み**（椅子座りへの転用ベースあり） |
| 体育館 (`gymnasium`) ステージ | **未実装** |

---

### キャラクター追加

| 項目 | 工数 | 理由 |
|---|---|---|
| ほのか（友人）の配置と会話 | **S** | `honoka` 定義・ダイアログ雛形が既存。MainScene に配置するだけ |
| 先輩の配置と会話 | **S** | `senior` 定義済み。学校ステージへの追加のみ |
| 両親（母158cm・父170cm）の配置 | **S** | `mother`/`father` 定義済み。`room` ステージに配置するだけ |

---

### ゲームイベント

| 項目 | 工数 | 理由・注意点 |
|---|---|---|
| ほのかとの出会いイベント（1学期） | **M** | 「おはよう→身長測ろう→保健室へ」の3ステップ分岐ダイアログ実装。学期トリガーの条件付きキューが必要 |
| バレー部先輩イベント（1学期） | **L** | **体育館ステージ新規作成**が必須。ステージ + 移動フロー + 先輩会話の3点セット |
| 夏休み急成長イベント（2学期） | **M** | `advance_term()` に夏休みフェーズを追加（+10cm ボーナス）。母親との相談ダイアログ。制服「小さくなった」視覚表現 |

---

### アニメーション

| 項目 | 工数 | 理由・注意点 |
|---|---|---|
| NPCを主人公が見下ろす（頭部角度） | **S** | NPC 側の `look_pitch` は実装済み。主人公の頭部を相手に向けるだけ |
| 椅子に座る・立ち上がる | **M** | `taiiku_suwari` を参考に新ポーズ追加。椅子高さに合わせた膝角度計算が必要 |
| 寝る・起きる・ベッドから立ち上がる | **L** | 現在の描画エンジンは縦向きのみ対応。横向き描画モードの新規設計が必要 |

---

### 🎯 推奨着手順

コアバリュー「視線の格差」「ライフステージの変遷」に直結するものから着手を推奨しますわ。

```
1. NPCを見下ろす表現 (S)       ← 高身長体験のコアフック。短時間で効果大
2. ほのか・先輩・両親の配置 (S×3) ← 定義済みなのでまとめて実装可能
3. ほのかとの出会いイベント (M)  ← ゲームの縦軸が動き出す
4. 夏休み急成長イベント (M)     ← ライフステージ変遷の体験に直結
5. 椅子に座るアニメーション (M)  ← 教室シーンの日常体験向上
6. バレー部先輩イベント (L)     ← 体育館ステージごと作る大型タスク
7. 寝るアニメーション (L)       ← 描画システム拡張が必要。後回し推奨
```

---
*Co-Authored-By: Claude Sonnet 3.7 <noreply@anthropic.com>*
*Co-Authored-By: gemini <218195315+gemini-cli@users.noreply.github.com>*
