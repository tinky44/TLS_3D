# AI 検討メモ (plan_by_agent)

このファイルはAIがアイデアや実装計画、既存コードの分析結果を書き溜めるためのメモ帳です。

---

## 現在のステータス（2026-03-08）

- **完了済み**: 基盤システム、体の基本描画（横向き・正面向き）、一部の顔の向き対応、基本的なUI構築、ゲームフロー（タイトル→キャラクリ→メイン）、自室ステージ、ステージ遷移、ESCメニューZ-orderバグ修正、ステータスオーバーレイ化（Qキー）、ゲーム導入IntroScene（MVP）。
- **着手中のタスク**: なし（次は提案2の顔クローズアップ演出や、ステージ拡張）。

---

## 次のステップ（Next Actions）

### Z. バグ修正：ESCメニューZ-order ✅ 完了（2026-03-08）
- `_ready()` の初期化順を `_setup_bubble()` → `_setup_pause_menu()` に変更
- pause_menu が ui_layer の最後の子になり、吹き出し（bubble_panel）の上に正しく描画されるようになった

### A. ゲームの導入（MVP）の実装（優先：高）
「朝起きて自分の部屋にいる」という自然な人生の流れを作るための導入の実装。
開発スピードを優先し、以下のアプローチを検討・実装する。

1. **IntroScene（導入シーン）の作成** ✅ 完了（2026-03-08）
   - 【提案1：暗転スタート（MVP）】実装済み
     - `godot-project/scenes/IntroScene.tscn` + `scripts/IntroScene.gd` を新規作成
     - 「朝…」「目が覚めた。」を順番にフェードイン・アウトで表示
     - テキスト完了後に暗転してMainSceneへ遷移
     - SaveSlotSelectScene（saveモードのみ）→ IntroScene → MainScene の流れ
   - 【提案2：顔のアップ（おすすめ）】→ 将来の拡張候補

### B. 自室ステージの追加（優先：中）
導入後にキャラクターが最初に立つ「自分の部屋」を構築する。

1. **配置する家具** ✅ 完了（2026-03-08）
   - ベッド（200cm）、本棚、学習机、窓、ランドセル、椅子、シーリングライト、ドア（見た目のみ）
   - キャラクリ後の開始ステージを `"myroom"` に変更済み

2. **シーン遷移ギミックの実装** ✅ 完了（2026-03-08）
   - `door_to_room`（x:600〜675）に近づいた際、`"room"` ステージへ遷移する処理を追加する
   - 実装方針：MainScene でプレイヤーがドアの x 範囲内に入ったら `Global.current_stage_id = "room"` → `_load_stage()` 再呼び出し
   - リビング側（room）の `poster`（x:1080〜1130）の位置に、自室へ戻るドアを追加する必要あり
   - 影響：`side_door`・`washstand` 以降のオブジェクトを右にずらす必要あり

### C. UIの改善：ステータス表示のオーバーレイ化 ✅ 完了（2026-03-08）
- サイドバーを初期非表示に変更。**Qキーでトグル表示**。
- 左上に「Q: ステータス」ヒントラベルを常時表示。
- カメラのXオフセット（-160）を0に修正（サイドバー常時表示が前提でなくなったため）。

---

## 将来的な展望・課題（Backlog）

### 1. 服を着せる方法の探求
- 描画方法の確立：
  - 「既製品」と「特注（完璧に合わせる）」の違いをどう描画で表現するか。丈が足りない様子などの視覚化。
- 関節の描画改善：
  - 脚などのジョイントボールを廃止し、線で滑らかに繋ぐ（`Line2D`の活用など）。
  - 服の描画について、矩形を動かした際にできる隙間を埋めるため、関節部に半円（カプセル状の端）を追加する。

### 2. 人生（学年）の導入とセーブ機能
- キャラ作成画面で「学年（保育園～高校）」を選択できるようにする。
- 学年に応じて服のプリセットが変わり、身長などのパラメータが自動設定・または可変になる仕組みの導入。
- 外の世界への移動（ステージ選択）は、家の「玄関のドア」を調べた際や、持ち物の「スマホ（予定表）」を開いたときにUIを表示する形式に改修する。
- ローカルセーブ・ロード機能基盤の作成。

---

## 開発メモ

### 身体パラメータの計算式 (`Global.gd`)
- `head = height / ratio`
- `leg = height * legRatio / 100`
- `arm = height - leg - head - (neck * 2)`
- `armLength = head * 2.7`

### 屈みの二分探索 (`CharacterPoseCalculator.gd`)
目標の高さ（px）を満たす最小の `t` (ポーズパラメータ) を反復探索。
- `t` (0~1): 直立からかがみ（背中を丸める）
- `t` (1~2): 更に深く屈む（膝を曲げる）
