# AI 検討メモ (plan_by_agent)

このファイルはAIがアイデアや実装計画、既存コードの分析結果を書き溜めるためのメモ帳です。

---

## 現在のステータス（2026-03-08）

- **完了済み**: 
  - 基盤システム、体の基本描画（横・正面・顔の向き対応）
  - 基本的なUI構築、ゲームフロー（タイトル→キャラクリ→メイン）
  - 自室ステージ・街・学校・駅などのステージ拡張と各ステージ間の双方向な移動アクション（シームレスな遷移基盤実装済み）
  - ESCメニューZ-orderバグ修正、ステータスオーバーレイ化、IntroScene導入
- **現在のフォーカス（未完了・着手予定）**: 
  - 比較対象となるNPCの実装・配置
  - 両親の身長設定の導入・親NPCの基礎

---

## 次のステップ（Next Actions）

### A. NPCの導入と配置（優先：高）
「相対的な身長の高さ」をゲーム内で実感させるため、プレイヤーと比較対象になるNPCを実装し配置する。

1. **NPCの描画基盤と実装**
   - プレイヤーと比較できる「一般的な身長のNPC（例：158cmなど）」をステージ上に配置する。
   - 現状の描画システム（CharacterDrawerなど）を拡張・または別ノード化し、NPCも同様の手法で柔軟なプロポーションで描画できるようにする。
   - NPC用の待機・歩行などの基本アニメーション制御の追加。

2. **NPCとのインタラクションの基礎**
   - 接近した際に近づいて見上げる・見下ろすなどの視線や姿勢の対応、あるいは簡単な会話UIなど。

### B. 両親の身長設定と遺伝コンテキストの導入
主人公がどのような環境（遺伝的な期待値）で育ったか、初期のコンテキストを持たせる仕組み。

1. **キャラクリ画面の拡張**
   - 「父親」「母親」の身長を入力またはランダム生成できるようにする。
   - 学年（後述）と合わせて、主人公への遺伝的影響や設定背景を決定づける。
   - ゆくゆくはこれをベースにして親NPCの見た目を自動生成し、作中に登場させる。

---

## 将来的な展望・課題（Backlog）

### 1. 服を着せる方法の探求
- 描画方法の確立：
  - 「既製品」と「特注（完璧に合わせる）」の違いをどう描画で表現するか。既製品だと丈が足りない様子などの視覚化。
- 関節の描画改善：
  - 脚などのジョイントボールを廃止し、線で滑らかに繋ぐ（`Line2D`の活用など）。
  - 服の描画において、矩形を動かした際にできる隙間を埋めるため、関節部に半円（カプセル状の端）を追加する。

### 2. 人生（学年）の導入とセーブ機能
- キャラ作成画面で「学年（保育園～高校）」を選択できるようにする。
- 学年に応じて服のプリセットが変わり、初期身長などのパラメータが自動設定・または可変になる仕組みの導入。
- ローカルセーブ・ロード機能基盤の作成。

---

## 開発メモ

### 身体パラメータの計算式 (`Global.gd`)
- `head = height / ratio`
- `leg = height * legRatio / 100`
- `arm = height - leg - head - (neck * 2)`
- `armLength = head * 2.7`

### 屈みの二分探索 (`CharacterPoseCalculator.gd`)
目標の高さ（px）を満たす最小の `t` (ポーズパラメータ) を反復探索。
- `t` (0~1): 直立からかがみ（背中を丸める）
- `t` (1~2): 更に深く屈む（膝を曲げる）
